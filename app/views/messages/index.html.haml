.wrapper
  .chat-side
    = render 'sidebar'
  .chat-main
    = render 'chat-main'
    .header
      = render partial: 'header' , colllection: @messages
    .messages
      = render partial: 'messages', collection: @messages
    .form
      =render 'form'
      